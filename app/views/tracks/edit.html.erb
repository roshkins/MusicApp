<h1> Fix this track! </h1>
<%= render :partial => "form", :locals => {:track => @track, :album => @album} %>
<%= button_to "Click for utter destruction", track_url(@track), :method => :delete, :confirm => "Are you sure you want to demolish this song? It hasn't harmed you... yet."%>
<a href="<%= track_url(@track) %>">Lost? Go back!</a>